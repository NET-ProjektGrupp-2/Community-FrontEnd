
import React from 'react'

export default function Logout() {
  return (
	// tell backend to end session to invalidate jwt token
	// clear local cache of any sensitive information
	<div>Logout</div>
  )
}
